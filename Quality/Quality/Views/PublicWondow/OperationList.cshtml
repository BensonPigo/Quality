@using DatabaseObject.Public

@{
    Layout = null;
    string title = ViewData["Title"] == null ? "Test Fail Mail" : ViewData["Title"].ToString();
    string TargetID = ViewData["TargetID"] == null ? string.Empty : ViewData["TargetID"].ToString();

}

@model List<Window_Operation>

<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<link href="~/ThirdParty/plugins/DataTables/datatables.min.css" rel="stylesheet" />
<link href="~/ThirdParty/SciCustom/css/TableStyle.css" rel="stylesheet" />
<script src="~/ThirdParty/plugins/DataTables/datatables.min.js"></script>

<style>

    ul {
        padding: 0;
    }


    .head {
        border-top: 2px solid black;
        text-align: center;
        font-size: 1.2em;
        background-color: lightgray;
    }
</style>

<fieldset style="width:700px;height:600px;">
    <legend>@title</legend>


    <div style="width:100%;height:90%;overflow-y: scroll;">
        <table id="MyTable" style="width:100%;max-height:95%;" class="table table-striped display">
            <thead>
                <tr>
                    <th> </th>
                    <th>Operation</th>
                    <th>Operator</th>
                    @*<th>ToAddress</th>*@
                    @*<th>CcAddress</th>*@
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><input type="checkbox" name="name" value="" GroupName="@item.Operation" /></td>
                        <td>
                            @item.Operation
                        </td>
                        <td>
                            @item.Name
                            <input type="hidden" colTyoe="Value" GroupName="@item.Operation" value="@item.EmployeeID" />
                            <input type="hidden" colTyoe="Text" GroupName="@item.Operation" value="@item.Name" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</fieldset>
<br />
<input id="btnSend" type="button" value="Send" />
<input id="btnExit" type="button" value="Exit" />
<script>


    $(function () {
        var table = $('#MyTable').DataTable({
            "filter": false,
            "paging": false,
            "order": false,
            "info": false,
            columns: [
                { target: 0 },
                { target: 1 },
                { target: 2 },
            ]
        });

        $("#btnSend").click(function () {

            //取得勾選的Group Name
            var GroupNameList = [];
            $("input[type=checkbox]:checked").each(function () {

                var GroupName = $(this).attr("GroupName");
                GroupNameList.push(GroupName);
            });


            var OperationObjs = [];
            for (var i = 0; i < GroupNameList.length; i++) {

                var operation = GroupNameList[i];
                var value = $("input[GroupName='" + operation + "'][colTyoe=Value]").val();
                var text = $("input[GroupName='" + operation + "'][colTyoe=Text]").val();

                var OperationObj = {
                    Operation: operation,
                    Value: value,
                    Text: text,
                }
                OperationObjs.push(OperationObj);
            }

            // 呼叫父視窗的GetTestFailMail Function，把值傳回去
            window.opener.GetOperationList(OperationObjs, '@TargetID');
            window.close();

            @*if (OperationObjs.length > 0) {
                // 呼叫父視窗的GetTestFailMail Function，把值傳回去
                window.opener.GetOperationList(OperationObjs, '@TargetID');
                window.close();
            }
            else {
                alert("Please choose Operation.");
            }*@

        });

        $("#btnExit").click(function () {
            window.close();
        });

    });

</script>